import 'package:flutter/material.dart';

class anehbet extends StatelessWidget {
  const anehbet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
